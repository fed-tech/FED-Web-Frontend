import React from "react";
import PropTypes from "prop-types";

const GoogleSignUp = ({ setLoad }) => {
  return <div>GoogleSignUp</div>;
};

GoogleSignUp.propTypes = {
  setLoad: PropTypes.func.isRequired,
};

export default GoogleSignUp;
